apiVersion: v1
kind: Service
metadata:
  name: gitea
  namespace: gitea
  labels:
    app: gitea
spec:
  type: ClusterIP
  selector:
    app: gitea
  ports:
    - name: web
      port: 3000
      targetPort: 3000
    - name: ssh
      port: 22
      targetPort: 22
